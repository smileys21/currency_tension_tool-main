# Dashboard runtime only — what the Streamlit app imports.
# (Data ingestion runs in the GitHub Action; see requirements-ingest.txt.)
streamlit>=1.36
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
matplotlib>=3.8
requests>=2.31
